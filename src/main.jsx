import React from "react";
import ReactDOM from "react-dom/client";
import VolunteerIntakeForm from "./VolunteerIntakeForm.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VolunteerIntakeForm />
  </React.StrictMode>
);
